# PRD: Project Architecture Mapper

## Проблема

**Для кого:** Разработчики, работающие с AI-агентами для рефакторинга кода (Roo Code, Cursor, Claude Code и т.д.)

**Боль:**
- AI-агенты не видят полную картину проекта без загрузки всех файлов (тратится куча токенов)
- Существующие инструменты (`madge`, `dependency-cruiser`) выдают либо огромный JSON, либо графы-картинки, которые LLM не может прочитать
- Разработчик не понимает, **где** в проекте архитектурные проблемы (God Objects, циклические зависимости, нарушения изоляции слоёв)
- Нет простого способа показать агенту "карту местности" перед началом рефакторинга

**Текущее решение:**
Разработчик вручную объясняет структуру проекта в промпте → теряет время, забывает детали, агент работает вслепую.

---

## Решение

**Что делает инструмент:**
Генерирует компактную текстовую карту проекта (`ARCHITECTURE.md`) с автоматическими пометками о проблемах архитектуры.

**Формат вывода:**
```markdown
# PROJECT CONTEXT MAP
Generated: 2026-01-20

Legend:
- [!] BIG = Файл >300 строк (кандидат на разделение)
- [!] CYCLE = Циклическая зависимость
- [!] LAYER-BREACH = Нарушение изоляции слоёв (FSD)
- [!] ORPHAN = Неиспользуемый код
- [!] FEATURE-CROSS = features импортирует другую features

/src
  /features
    /auth
      index.ts -> (@/shared/api, ./model)
      /model
        auth.store.ts [!] BIG (450 lines) [!] GOD-MODULE -> (@/shared/api, @/entities/user, ...)
      /ui
        LoginForm.tsx -> (./model, @/shared/ui)
    /payments
      /model
        payment.store.ts [!] FEATURE-CROSS -> (@/features/auth/model) [!] CYCLE
```

---

## Как это работает

### Входные данные
- Корневая папка проекта (обычно `./src`)
- Конфигурация игнорирования (`node_modules`, `.git`, `dist`, etc.)
- Пороги для детекции (размер файла, глубина импортов)
- Правила изоляции слоёв (опционально, для FSD-проектов)

### Процесс
1. **Сканирование:** Рекурсивный обход файловой системы
2. **Анализ каждого файла:**
   - Подсчёт строк кода
   - Парсинг импортов (regex для `import ... from '...'`)
   - Определение слоя по пути (для FSD: `features/`, `entities/`, `shared/`)
3. **Детекция паттернов:**
   - `BIG` — файл превышает N строк
   - `CYCLE` — A импортирует B, B импортирует A (граф зависимостей)
   - `LAYER-BREACH` — нарушение иерархии слоёв
   - `GOD-MODULE` — >10 входящих зависимостей
   - `FEATURE-CROSS` — одна фича импортирует другую напрямую
   - `DEEP-PATH` — импорт через `../../../` (>2 уровней)
   - `ORPHAN` — экспорты без импортов
4. **Форматирование:** Отступы для вложенности, пометки `[!]`, сокращение списка импортов

### Выходные данные
Файл `ARCHITECTURE.md` в корне проекта.

---

## Технические решения

### Почему Node.js скрипт, а не CLI-тул?
- **Простота запуска:** `node map-project.js` без установки зависимостей
- **Кастомизация:** Разработчик может легко править под свой проект
- **Zero dependencies:** Только `fs` и `path` из стандартной библиотеки
- **Скорость:** Для проектов <5000 файлов работает мгновенно

### Почему Markdown, а не JSON?
- **Читаемость:** LLM и человек понимают без парсинга
- **Компактность:** 100-200 строк вместо 10000 строк JSON
- **Приоритизация:** Пометки `[!]` сразу видны
- **AI Preamble:** Можно встроить инструкции для агента в начало файла

### Парсинг импортов: Regex vs AST

**MVP (v0.1-0.2): Улучшенный Regex**
- Поддержка многострочных импортов (склейка незавершённых строк)
- Re-exports: `export { x } from './y'`
- Динамические импорты: `await import(...)` (best effort)
- **Плюсы:** Zero dependencies, работает везде
- **Минусы:** Может пропустить edge cases (5-10% импортов)

**v0.3+: TypeScript Compiler API** (опционально)
- Если в проекте есть `typescript` в devDependencies — используем его
- 100% точность парсинга
- Без добавления новых зависимостей
- **Компромисс:** Чуть медленнее, но надёжнее

### Детекция циклов: Конкретность вместо флага

**Плохо:**
```markdown
index.ts [!] CYCLE
```

**Хорошо:**
```markdown
index.ts [!] CYCLE (-> model/store.ts -> api/client.ts -> index.ts)
```

Это критично для агента — он понимает, **как именно** разорвать цикл.

### Визуальный формат: Tree-символы

**Текущий (отступы):**
```markdown
/features
  /auth
    index.ts
```

**Улучшенный (tree-ветки):**
```markdown
/features/auth  (Layer: Feature)
  ├── index.ts        [!] CYCLE (-> ./model -> index)
  ├── ui/
  │   └── LoginForm.tsx
  └── model/
      └── auth.store.ts  (450 loc) [!] GOD-MODULE [!] HIGH-CHURN
          -> @shared/api, @entities/user
```

**Почему:** LLM отлично парсит `├──`, визуально понятнее для человека.

### Ограничения (намеренные)
- **Только локальные импорты:** Игнорируем `node_modules`, фокус на архитектуре проекта
- **Топ-5 импортов на файл:** Чтобы не раздувать карту (остальные в `...`)
- **Эвристики вместо точности:** `[!] BIG` — это подсказка, не приговор
- **Быстрота vs 100% точность:** Regex может пропустить 5% импортов, но работает мгновенно

---

## Целевая аудитория

### Primary Users
**Junior/Middle разработчики, работающие с легаси-кодом**
- Нужно быстро разобраться в чужом проекте
- Планируют рефакторинг с помощью AI-агентов
- Не хотят читать 500 файлов руками

### Secondary Users
**Tech Leads / Архитекторы**
- Проводят Code Review больших PR
- Ищут архитектурные проблемы перед миграцией (например, на FSD)
- Нужна "рентген-снимок" проекта

### AI Agent (как пользователь)
- Получает контекст проекта за 1 файл вместо 100
- Видит проблемные зоны благодаря пометкам `[!]`
- Может построить план рефакторинга на основе карты

---

## Killer Features (Темпоральный анализ)

### Feature 1: Hotspots через Git Integration

**Проблема:** LLM видит код статично. Не знает, что `utils.ts` правят 5 раз в день, а `legacy.ts` не трогали 3 года.

**Решение:** Интеграция с git log
```bash
git log --format=oneline --since="1 month ago" -- path/to/file.ts | wc -l
```

**Вывод:**
```markdown
payment.controller.ts  (450 loc) [!] BIG [!] HIGH-CHURN (50 commits/month)
legacy-adapter.ts      (200 loc) [!] STALE (last change: 2 years ago)
```

**Ценность:** Прямой сигнал агенту — "Этот файл болит **сейчас**, начни с него".

**Приоритет:** v0.3 (требует git в системе, но это есть в 99% проектов)

### Feature 2: AI Preamble (Программирование агента)

**Идея:** Встроить в начало `ARCHITECTURE.md` инструкцию для LLM

```markdown
# AI INSTRUCTIONS (для агентов рефакторинга)

Этот файл — карта проекта. Используй его ДО запроса файлов.

**Легенда приоритетов:**
1. [!] CYCLE — КРИТИЧНО. Разорви цикл перед любым рефакторингом
2. [!] HIGH-CHURN + [!] BIG — начни декомпозицию с этих файлов (они болят команду)
3. [!] LAYER-BREACH — строгий запрет, требует инверсии зависимостей
4. [!] STALE — низкий приоритет, не трогай без явного запроса

**Правила работы:**
- Не запрашивай `read_file` для всех файлов подряд
- Используй эту карту для планирования
- Спрашивай у пользователя перед чтением >5 файлов

---
# PROJECT MAP
...
```

**Ценность:** Агент сам понимает, как работать с картой. Меньше ошибок, меньше токенов.

### Feature 3: Focus Mode для больших проектов

**Проблема:** Проект на 10k файлов → карта на 20k токенов → не влезает в контекст.

**Решение:** Режимы детализации

**Standard mode** (по умолчанию):
```markdown
/features/auth/     (15 files, 3 [!] issues)
/features/payments/ (8 files, 1 [!] issue)
```

**Focus mode** (`--focus=features/auth`):
```markdown
/features/auth/  (Layer: Feature)
  ├── index.ts [!] CYCLE (-> ./model -> index)
  ├── ui/
  │   └── LoginForm.tsx (120 loc)
  └── model/
      └── auth.store.ts (450 loc) [!] BIG [!] GOD-MODULE [!] HIGH-CHURN
          -> @shared/api
          -> @entities/user
          
/features/payments/  (collapsed, 8 files)
```

**Команда:**
```bash
node map-project.js --focus=features/auth --depth=2
```

**Приоритет:** v0.3-0.4 (нужен для монолитов)

### Метрики эффективности
- ⏱️ **Скорость генерации:** <5 секунд для проекта в 2000 файлов
- 📊 **Размер вывода:** <300 строк для среднего проекта (vs 10k+ строк в `madge --json`)
- 🎯 **Точность детекции:** >80% файлов с `[!] BIG` действительно нуждаются в рефакторинге
- 🤖 **Экономия токенов:** В 20-50 раз меньше токенов для загрузки контекста в LLM

### Качественные показатели
- ✅ Разработчик понимает структуру проекта за 2 минуты чтения карты
- ✅ AI-агент может составить план рефакторинга без дополнительных вопросов
- ✅ Обнаруживает как минимум 1 критическую проблему в 70% проектов

---

## Roadmap

### MVP (v0.1) — Базовая карта
- [x] Рекурсивное сканирование файлов
- [x] Подсчёт строк
- [x] Парсинг локальных импортов
- [x] Детекция `[!] BIG`
- [x] Вывод в Markdown

### v0.2 — КРИТИЧНЫЕ детекторы (безопасность рефакторинга)
- [ ] `[!] CYCLE` — циклические зависимости с путём цикла `(-> A -> B -> C -> A)`
- [ ] `.gitignore` — респект к игнорируемым файлам проекта
- [ ] Улучшенный regex парсинг (многострочные импорты, re-exports)

### v0.3 — Темпоральный анализ (git integration)
- [ ] `[!] HIGH-CHURN` — файлы с частыми изменениями (git log)
- [ ] `[!] STALE` — код без изменений >6 месяцев
- [ ] Режимы вывода: `--depth` и `--focus=path` для больших проектов

### v0.4 — Архитектурные проверки
- [ ] `[!] LAYER-BREACH` — нарушения FSD
- [ ] `[!] FEATURE-CROSS` — cross-feature импорты
- [ ] `[!] GOD-MODULE` — входящие зависимости >10
- [ ] `[!] ORPHAN` — мёртвый код
- [ ] `[!] DEEP-PATH` — глубокие относительные пути
- [ ] `[!] BARREL-HELL` — index.ts с >20 экспортами

### v1.0 — Production Ready
- [ ] CLI с параметрами (`--output`, `--ignore`, `--rules`, `--focus`, `--depth`)
- [ ] Конфигурация через `mapper.config.js`
- [ ] Интеграция с pre-commit хуками
- [ ] Diff-режим (сравнение ARCHITECTURE.md до/после рефакторинга)
- [ ] Watch mode для continuous analysis
- [ ] Визуализация в HTML (опционально)

---

## Альтернативы и почему не они

| Инструмент | Почему не подходит |
|------------|-------------------|
| `madge` | Выводит JSON на 10k строк или PNG-граф. LLM не может прочитать картинку, JSON слишком избыточен |
| `dependency-cruiser` | Мощный, но сложный в настройке. Нужен конфиг на 100 строк. Вывод не оптимизирован для LLM |
| `tree` (Unix) | Просто список файлов, без метаданных и анализа зависимостей |
| Ручной анализ | Долго, субъективно, невоспроизводимо |

---

## Технический стек

- **Язык:** JavaScript (Node.js)
- **Зависимости:** Нет (только встроенные `fs`, `path`)
- **Формат вывода:** Markdown
- **Парсинг кода:** RegExp (для MVP, в будущем можно AST через `@babel/parser`)
- **Граф зависимостей:** Adjacency list в памяти (для детекции циклов)

---

## Примеры использования

### Кейс 1: Новичок в легаси-проекте
```bash
# Склонировал проект, хочу понять структуру
node map-project.js

# Читает ARCHITECTURE.md
# Видит: features/auth/model/auth.store.ts [!] BIG (800 lines) [!] GOD-MODULE
# Понимает: это главная проблемная зона
```

### Кейс 2: Рефакторинг с Roo Code
```bash
# Генерирует карту
node map-project.js

# Промпт в Roo Code:
"Прочитай ARCHITECTURE.md. Найди все файлы с [!] LAYER-BREACH 
и предложи план миграции на чистую FSD-структуру."
```

### Кейс 3: Code Review
```bash
# Перед мержем большого PR
git checkout feature/new-auth
node map-project.js
mv ARCHITECTURE.md ARCHITECTURE-after.md

git checkout main
node map-project.js
mv ARCHITECTURE.md ARCHITECTURE-before.md

# Сравнивает diff
diff ARCHITECTURE-before.md ARCHITECTURE-after.md
# Видит: добавилось 3 новых [!] CYCLE → PR отклонён
```

---

## Конфигурация (будущая версия)

```javascript
// mapper.config.js
module.exports = {
  rootDir: './src',
  output: 'ARCHITECTURE.md',
  ignore: ['node_modules', 'dist', '*.test.ts'],
  rules: {
    bigFile: 300, // строк
    godModule: 10, // входящих зависимостей
    deepPath: 3,   // уровней ../
  },
  layers: {
    // FSD иерархия
    'app': ['pages', 'widgets', 'features', 'entities', 'shared'],
    'pages': ['widgets', 'features', 'entities', 'shared'],
    // ...
  }
};
```

---

## Риски и ограничения

| Риск | Вероятность | Митигация |
|------|-------------|-----------|
| **Regex пропускает импорты** (многострочные, динамические, re-exports) | Высокая | v0.1: Склейка незавершённых строк. v0.3: TypeScript Compiler API если доступен |
| **Ложные срабатывания** `[!] BIG` для легитимно больших файлов | Средняя | Настраиваемые пороги через конфиг |
| **Неконкретные циклы** — агент не понимает, как разорвать | Высокая | Вывод полного пути цикла: `[!] CYCLE (-> A -> B -> A)` |
| **Карта раздувается** на монолитах >10k файлов | Средняя | Режимы `--depth` и `--focus` для выборочной детализации |
| **Медленная работа** на проектах >10k файлов | Низкая | Кеширование результатов, инкрементальное сканирование |
| **Проект не использует FSD** → бесполезны проверки LAYER-BREACH | Средняя | Детекция включается опционально через конфиг |
| **Git не доступен** для темпорального анализа | Низкая | Graceful fallback без `[!] HIGH-CHURN` |

---

## FAQ

**Q: Зачем это, если есть ESLint?**
A: ESLint проверяет синтаксис и стиль. Мы проверяем архитектуру и генерируем карту для AI.

**Q: Можно ли использовать для не-FSD проектов?**
A: Да. Детекция `BIG`, `CYCLE`, `GOD-MODULE` работает для любой структуры. FSD-проверки опциональны.

**Q: Как часто запускать?**
A: При старте рефакторинга, перед крупными PR, раз в спринт для мониторинга.

**Q: Поддержка TypeScript/Python/Go?**
A: MVP — только JS/TS (import syntax). В будущем можно добавить парсеры для других языков.

---

## Вывод

**Инструмент решает конкретную боль:** дать AI-агенту и разработчику компактную, понятную карту проекта с автоматическими пометками о проблемах.

**Философия:** Лучше быстрая эвристика с 80% точностью, чем медленный идеальный анализ, который никто не запустит.

**Целевая метрика:** Разработчик должен понять структуру проекта за 2 минуты чтения `ARCHITECTURE.md`, а AI-агент — за 1 запрос.