#!/usr/bin/env node

/**
 * Binary entry point for the `mapper` command.
 * This file is compiled to `dist/cli/main.js` and referenced in package.json's "bin".
 */
import { main } from './index.js';

main();
